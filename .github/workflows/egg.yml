name: a

on: push

    # schedule:
    #  - cron: "0 1 * * *"

jobs:
    job1:
        runs-on: windows-2025
        steps:
         - uses: actions/checkout@v2
    
         - run: python --version
         - run: python ./a.py
